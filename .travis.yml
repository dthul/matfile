language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
cache: cargo
script:
  - cd "${TRAVIS_BUILD_DIR}/matfile"
  - cargo build
  - cargo test --all-features